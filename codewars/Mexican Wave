function wave(str){
var arr = [], i;
 for (i = 0; i < str.length; i++) {
  if (str[i] === ' ') continue;
        arr.push(Array.from(str, (c, j) => i === j ? c.toUpperCase() : c).join(''));
    }
    return arr;
}
